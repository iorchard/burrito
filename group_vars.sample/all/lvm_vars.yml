---
# Physical volume devices
# if you want to use multiple devices,
#   use comma to list devices (e.g. "/dev/sdb,/dev/sdc,/dev/sdd")
lvm_devices: "/dev/sdb"

########################
# Do Not Edit below!!! #
########################
# cinder lvm volume driver
lvm_volume_driver: "cinder.volume.drivers.lvm.LVMVolumeDriver"
lvm_vg_name: "cinder-volume"
...
