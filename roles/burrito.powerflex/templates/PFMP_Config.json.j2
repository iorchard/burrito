{
  "Nodes":
  [
    {
      "hostname": "pfmp-mvm1",
      "ipaddress": "{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 1) }}"
    },
    {
      "hostname": "pfmp-mvm2",
      "ipaddress": "{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 2) }}"
    },
    {
      "hostname": "pfmp-mvm3",
      "ipaddress": "{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 3) }}"
    }
  ],

  "ClusterReservedIPPoolCIDR" : "10.42.0.0/23",
  "ServiceReservedIPPoolCIDR" : "10.43.0.0/23",
  "RoutableIPPoolCIDR" : [
    {"mgmt": "{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 4) }}-{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 6) }}"},
    {"data": "{{ pfmp_storage_net_segment }}.{{ (pfmp_vm_start_ip|int + 4) }}-{{ pfmp_storage_net_segment }}.{{ (pfmp_vm_start_ip|int + 6) }}"}
  ],
  "PFMPHostname" : "pfmp.cluster.local",
  "PFMPHostIP" : "{{ pfmp_mgmt_net_segment }}.{{ (pfmp_vm_start_ip|int + 4) }}"
}
