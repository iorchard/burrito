--- kubespray/roles/kubernetes-apps/registry/templates/registry-rs.yml.j2	2025-07-25 17:51:22.737961011 +0900
+++ patches/kubespray/roles/kubernetes-apps/registry/templates/registry-rs.yml.j2	2025-07-25 17:50:30.550961011 +0900
@@ -1,6 +1,6 @@
 ---
 apiVersion: apps/v1
-kind: ReplicaSet
+kind: Deployment
 metadata:
   name: registry
   namespace: {{ registry_namespace }}
@@ -9,6 +9,8 @@
     version: v{{ registry_image_tag }}
     addonmanager.kubernetes.io/mode: Reconcile
 spec:
+  strategy:
+    type: Recreate
 {% if registry_storage_class != "" and registry_storage_access_mode == "ReadWriteMany" %}
   replicas: {{ registry_replica_count }}
 {% else %}
@@ -24,7 +26,8 @@
         k8s-app: registry
         version: v{{ registry_image_tag }}
     spec:
-      priorityClassName: {% if registry_namespace == 'kube-system' %}system-cluster-critical{% else %}k8s-cluster-critical{% endif %}{{ '' }}
+      nodeSelector:
+        node-role.kubernetes.io/control-plane: ""
       serviceAccountName: registry
       securityContext:
         fsGroup: 1000
